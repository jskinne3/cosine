<p id="notice"><%= notice %></p>

<h1><%= @h1 %></h1>

<%= paginate @books %>

<table>
  <thead>
    <tr>
      <th>Osp work</th>
      <th>Title</th>
      <th>Authors</th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.osp_work_id %></td>
        <td><%= book.title %></td>
        <td><%= book.authors %></td>
      </tr>
    <% end %>
  </tbody>
</table>
